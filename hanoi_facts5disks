(disk1 Object)
(disk2 Object)
(disk3 Object)
(disk4 Object)
(disk5 Object)
(pegA Object)
(pegB Object)
(pegC Object)

(preconds
    (on disk1 pegA)
    (on disk2 disk1)
    (on disk3 disk2) 
    (on disk4 disk3) 
    (on disk5 disk4)
    
    (clear disk5)
    (clear pegB)
    (clear pegC)

    (smaller disk1 pegA)
    (smaller disk1 pegB)
    (smaller disk1 pegC)
    (smaller disk2 pegA)
    (smaller disk2 pegB)
    (smaller disk2 pegC)
    (smaller disk3 pegA)
    (smaller disk3 pegB)
    (smaller disk3 pegC)
    (smaller disk4 pegA)
    (smaller disk4 pegB)
    (smaller disk4 pegC)
    (smaller disk5 pegA)
    (smaller disk5 pegB)
    (smaller disk5 pegC)

    (smaller disk2 disk1)

    (smaller disk3 disk1)    
    (smaller disk3 disk2)
    
    (smaller disk4 disk1)
    (smaller disk4 disk2)
    (smaller disk4 disk3)

    (smaller disk5 disk1)
    (smaller disk5 disk2)
    (smaller disk5 disk3)
    (smaller disk5 disk4)
)

(effects
    (on disk1 pegC)
    (on disk2 disk1)
    (on disk3 disk2) 
    (on disk4 disk3)     
    (on disk5 disk4)     
)